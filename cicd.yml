version: "1"
stages:
  main:
    files:
      - docker-compose.yml
  image:
    files:
      - docker-compose.image.yml
  fail:
    files:
      - docker-compose.fail.yml
  long:
    detach: true
    files:
      - docker-compose.long.yml
